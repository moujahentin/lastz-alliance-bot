[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "lastz-alliance-assistant"
version = "0.1.0"
description = "Multi-alliance Discord bot for Last Z: Survival Shooter communities."
readme = "README.md"
requires-python = ">=3.13"

dependencies = [
    "discord.py==2.7.1",
    "python-dotenv==1.2.3",
    "SQLAlchemy==2.0.52",
    "alembic==1.19.1",
]

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
